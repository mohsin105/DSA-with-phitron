#include<bits/stdc++.h>
using namespace std;
int main()
{
    int q;
    cin>>q;
    vector<int>v;
    while(q--)
    {
        int op;
        cin>>op;
        if(op==1)
        {
            int n;
            cin>>n;
            v.push_back(n);
        }
    }
    return 0;
}